refresh_rate: 30 seconds

appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d(%l:%M:%S)} | {h({l:<5})} | {({f}:{L}):64.64}{n}{h({m})}{n}{n}"

root:
  level: warn
  appenders:
    - stdout

loggers:
  skeleton:
    level: trace
    appenders:
      - stdout
    additive: false
